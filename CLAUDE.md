# TipAI Agent Team Skill — 项目全局约束

## 核心约束

### 1. 原子任务驱动
所有开发必须严格按照 `TASKS.md` 中的原子任务顺序执行。禁止跳过任务、合并任务、或偏离任务范围。

### 2. 完成一项，测试一项
- 每完成一个原子任务，立即编写对应测试
- 测试未通过 → 该任务不得勾选 `[x] 完成`
- 禁止批量完成后统一补测试

### 3. QA 验证
- 每个任务完成后，必须写 QA 验证报告
- QA 报告内容: 测试用例数 / 通过率 / 覆盖场景 / 边界条件
- QA 未通过 → 不得勾选 `[x] 验证`
- 如果任务是纯数据结构定义(Schema)，QA = Schema 校验用例通过

### 4. 勾选规则
```
[ ] 完成  → 代码实现完毕 + 自测通过
[x] 完成

[ ] 验证  → QA 报告通过 + 符合度确认无偏离
[x] 验证
```

### 5. 完成说明
每个任务打勾后，必须在任务下方追加完成说明：

```
### T-XXXX: 任务名
- [x] 完成 — 2026-05-13，实现文件: xxx, 自测: 通过
- [x] 验证 — 2026-05-13，QA: 3个用例全通过，边界覆盖: 空值/超时/并发
```

### 6. 进度更新
每次完成任务后，必须同步更新顶部进度总览表中的数字。

### 7. 禁止行为
- 禁止跳过测试直接勾选完成
- 禁止 QA 未做就勾选验证
- 禁止批量勾选（必须逐任务写说明）
- 禁止实现偏离 SPEC.md 定义

### 8. 提交粒度
每完成一个原子任务 → 一次独立 commit → 一次 push。
